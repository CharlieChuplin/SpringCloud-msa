server:
  port: 8761

spring:
  application:
    name: discovery-service

eureka:
  client:
    register-with-eureka: false # Eureka 서버에 자신을 등록하지 않는 설정
    fetch-registry: false       # Eureka 서버에서 레지스트리를 가져오지 않음